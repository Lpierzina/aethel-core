/target

/aethel_core.component.wasm

# Built by the component-tests workflow step; not committed.

# Generated by build.rs on a native build. Never committed, and must not reach
# the published crate: an untracked directory is swept into `cargo package`, so
# without this a stale local wasm binary ships inside a source package.
/dist/
